import create from "zustand/react";

/* const styleStore = create((set) => ({
    props: {},
    setProp: () => set((state) => ({ bears: state.bears + 1 })),
    removeAllBears: () => set({ bears: 0 }),
  })) */
